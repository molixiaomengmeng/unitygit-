using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("你们好呀，各位小伙伴");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
